<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="./css/stylehome.css">
    <title>Edit Room</title>
    <style>
        /* General Styles */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Arial', sans-serif;
            background-color: #f8f9fa;
            /* Light background for a clean look */
            color: #495057;
            /* Dark gray text color for better readability */
            line-height: 1.6;
        }

        header {
            background-color: #007bff;
            /* Blue background for the header */
            padding: 1rem 0;
            color: #fff;
        }

        nav .navbar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 0 20px;
        }

        nav .navbar a {
            color: #fff;
            text-decoration: none;
            padding: 10px;
            font-size: 16px;
            transition: background 0.3s;
        }

        nav .navbar a:hover {
            background-color: #0056b3;
            /* Darker blue on hover */
        }

        /* Main Content */
        .main-content {
            padding: 30px;
            max-width: 800px;
            margin: 50px auto;
            background-color: #fff;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            /* Soft shadow */
            border-radius: 8px;
            animation: fadeIn 1s ease-in-out;
        }

        h1 {
            font-size: 26px;
            margin-bottom: 20px;
            color: #007bff;
            /* Blue heading for emphasis */
        }

        form {
            display: flex;
            flex-direction: column;
        }

        label {
            margin-bottom: 8px;
            font-size: 16px;
            color: #495057;
            /* Dark gray color for labels */
        }

        input,
        select {
            padding: 10px;
            margin-bottom: 20px;
            border: 1px solid #ced4da;
            /* Light gray border for input fields */
            border-radius: 4px;
            font-size: 16px;
        }

        input[type="number"] {
            -moz-appearance: textfield;
        }

        input[type="number"]::-webkit-outer-spin-button,
        input[type="number"]::-webkit-inner-spin-button {
            -webkit-appearance: none;
            margin: 0;
        }

        button {
            padding: 12px;
            background-color: #28a745;
            /* Green button for a positive action */
            color: #fff;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-size: 16px;
            transition: background-color 0.3s ease;
        }

        button:hover {
            background-color: #218838;
            /* Darker green on hover */
        }

        button:active {
            background-color: #1e7e34;
            /* Even darker green when button is clicked */
        }

        /* Responsive Styles */
        @media (max-width: 768px) {
            .main-content {
                margin: 20px;
                padding: 20px;
            }

            h1 {
                font-size: 22px;
            }

            form {
                padding: 10px;
            }

            input,
            select,
            button {
                font-size: 14px;
            }
        }

        /* Animation */
        @keyframes fadeIn {
            0% {
                opacity: 0;
            }

            100% {
                opacity: 1;
            }
        }

        /* Footer */
        footer {
            text-align: center;
            padding: 20px;
            background-color: #007bff;
            /* Dark footer for contrast */
            color: #fff;
            position: fixed;
            width: 100%;
            bottom: 0;
        }

        /* Additional Styling for Forms */
        form label {
            margin-bottom: 10px;
            font-size: 18px;
            color: #495057;
        }

        input,
        select {
            font-size: 16px;
            margin-bottom: 20px;
        }

        button {
            font-size: 18px;
            cursor: pointer;
        }

        button:hover {
            background-color: #218838;
        }
    </style>
</head>

<body>
    <header>
        <nav class="navbar">

        </nav>
    </header>

    <main class="main-content">
        <h1>Edit Room - <%= room.roomNumber %>
        </h1>

        <form action="/edit-room/<%= room.roomNumber %>" method="POST">
            <label for="status">Room Status:</label>
            <select name="status" id="status">
                <option value="Available" <% if (room.status==='Available' ) { %>selected<% } %>>Available</option>
                <option value="Booked" <% if (room.status==='Booked' ) { %>selected<% } %>>Booked</option>
            </select>
 
            <button type="submit">Save Changes</button>
        </form>
    </main>

    <footer>
        <p>&copy; 2024 Hotel Rooms Management System. All rights reserved.</p>
    </footer>
</body>

</html>